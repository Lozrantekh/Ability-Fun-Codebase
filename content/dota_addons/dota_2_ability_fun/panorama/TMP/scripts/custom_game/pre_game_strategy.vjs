"use strict";$.Msg("pre game strategy!")var CUSTOM_ABILITIES_LOADED = falsefunction HideBasicHeroSpells(){       if (Game.GameStateIsBefore(DOTA_GameState.DOTA_GAMERULES_STATE_STRATEGY_TIME) && !CUSTOM_ABILITIES_LOADED) {        $.Schedule(1, HideBasicHeroSpells)        return    }    let playerID = Players.GetLocalPlayer()    if (Players.IsValidPlayerID(playerID)) {        let abilities = GetHUDPanelByID("SelectedHeroAbilities")        let abTargets = GetHUDPanelByID("SelectedHeroAbilitiesHitTargets")            if (abilities != null && abTargets != null) {            abTargets.style.visibility = "collapse";            abilities.style.visibility = "collapse";            abTargets.RemoveAndDeleteChildren();            abilities.RemoveAndDeleteChildren();                let pickedAbilities = CustomNetTables.GetTableValue( "players_ability_picks", playerID.toString() );                if (pickedAbilities !== null && pickedAbilities !== undefined) {                        let pickedAbilitiesData = Object.values(pickedAbilities)                    for (let index = 0; index < pickedAbilitiesData.length; index++) {                    const abilityData = pickedAbilitiesData[index];                    const abilityName = abilityData["ability_name"] || "";                         if ( abilityName != ""){                        let container = $.CreatePanel("Panel", abTargets, "")                        if (container != null) {                            container.SetHasClass("AbilityIconContainer", true)                                let abilityPanel = $.CreatePanel("DOTAAbilityImage", container, "");                                if (abilityPanel != null) {                                abilityPanel.hittest = true                                abilityPanel.abilityname = abilityName                                abilityPanel.SetPanelEvent("onmouseover", function () {                                    $.DispatchEvent("DOTAShowAbilityTooltip", abilityPanel, abilityName);                                });                                    abilityPanel.SetPanelEvent("onmouseout", function () {                                    $.DispatchEvent("DOTAHideAbilityTooltip");                                });                            }                            }                    }                }                CUSTOM_ABILITIES_LOADED = true                abTargets.style.visibility = "visible";            }        }    }}HideBasicHeroSpells();